[user]
  name = fenze
  email = contact@fenze.dev

[push]
  default = simple

[init]
  defaultBranch = master

[alias]
  contrib = shortlog -n -s
  remotes = remote -v
  praise = blame
  verify = log --show-signature
  patch = apply --reject

[push]
  default = current

[pull]
  rebase = false
